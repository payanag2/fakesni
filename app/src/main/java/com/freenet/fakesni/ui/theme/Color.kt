package com.freenet.fakesni.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds — deep blacks
val Background     = Color(0xFF060606)
val Surface        = Color(0xFF0E0E0E)
val CardBackground = Color(0xFF121212)
val SurfaceVariant = Color(0xFF1A1A1A)

// Primary — high-contrast white/near-white for interactive elements
val Primary        = Color(0xFFF0F0F0)
val PrimaryDim     = Color(0xFF282828)

// Accent colors for small beautiful details
val IconTeal       = Color(0xFF4ADE80)   // section icons — matches running green
val TimestampColor = Color(0xFF2D6B4A)   // log timestamps
val LogInfoColor   = Color(0xFF3A9E6A)   // [*] info lines
val FocusBlue      = Color(0xFF4ADE80)   // focused field border — matches running green

// Status colors
val Success        = Color(0xFF4ADE80)   // running dot
val Error          = Color(0xFFF87171)   // stop / error

// Text hierarchy
val OnBackground   = Color(0xFFF2F2F2)
val OnSurface      = Color(0xFF909090)
val OnSurfaceDim   = Color(0xFF616161)

// Structure
val BorderColor    = Color(0xFF2C2C2C)
val DividerColor   = Color(0xFF1A1A1A)
